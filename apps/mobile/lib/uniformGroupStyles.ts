/** Shared dimensions so sibling controls in a group stay visually equal. */

export const UNIFORM_SEGMENT_HEIGHT = 44;

export const UNIFORM_ROW_MIN_HEIGHT = 60;

export const UNIFORM_CHIP_FONT_SIZE = 12;

/** Inner segment pill height inside a padded track (track padding ≈ 4×2). */
export const UNIFORM_SEGMENT_INNER_HEIGHT = UNIFORM_SEGMENT_HEIGHT - 8;

/** Horizontal button pairs (confirm dialogs, etc.). */
export const UNIFORM_ACTION_BUTTON_MIN_HEIGHT = 48;

/** List section headers (date groups, merchant sections). */
export const UNIFORM_SECTION_HEADER_MIN_HEIGHT = 22;
