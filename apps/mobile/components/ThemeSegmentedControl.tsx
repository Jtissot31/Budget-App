/**
 * Canonical animated segmented control for the Budget Tracker theme kit.
 *
 * Re-exports {@link SegmentedTabs} with liquid sliding-pill animation (spring + stretch/squash).
 * Import from here or `@/components/SegmentedTabs` — both resolve to the same component.
 *
 * @see constants/theme.ts — `liquidSegmentedSpring`, `segmentedTabBarDark`, theme kit docs
 */
export { SegmentedTabs as ThemeSegmentedControl, SegmentedTabs } from './SegmentedTabs';
